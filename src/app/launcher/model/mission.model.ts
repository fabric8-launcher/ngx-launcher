export class Mission {
  description?: string;
  id: string;
  name: string;
  metadata?: any;
  prerequisite?: string;
  runtimes: string[];
  suggested?: boolean;
  url?: string;
  showMore?: boolean;
}
